/** Encabezado estándar de tabla (tokens NovaFact). */
export const TABLE_HEAD =
  'border-b border-border bg-muted/50 text-foreground';

/** Encabezado sticky para tablas con scroll. */
export const TABLE_HEAD_STICKY =
  'sticky top-0 z-10 border-b border-border bg-muted/50 text-foreground backdrop-blur supports-[backdrop-filter]:bg-muted/40';

/** Encabezado compacto (pickers, tablas de facturación). */
export const TABLE_HEAD_COMPACT =
  'border-b border-border bg-muted/40 text-left text-xs uppercase tracking-[0.14em] text-muted-foreground';
